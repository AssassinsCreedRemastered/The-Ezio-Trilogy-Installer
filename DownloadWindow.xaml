<Window x:Class="The_Ezio_Trilogy_Installer.DownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:The_Ezio_Trilogy_Installer"
        mc:Ignorable="d"
        Title="DownloadWindow" Height="150" Width="300" Background="#00000000" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" Foreground="{x:Null}">
    <Border Background="#FFDADADA"
            BorderBrush="Gray"
            BorderThickness="1"
            CornerRadius="10">
        <Grid Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Rectangle Width="Auto" Height="Auto" RadiusX="10" RadiusY="10" Grid.ColumnSpan="4" Grid.RowSpan="4">
            </Rectangle>
            <TextBlock Grid.Row="0" x:Name="Status" Text="" FontSize="18" TextAlignment="Center" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <ProgressBar x:Name="Progress" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="20"></ProgressBar>
        </Grid>
    </Border>
</Window>
